# Context Bridge v19 — 전문가형 맥락 모델

## 입력 원칙
사용자는 태그를 입력하지 않는다. 제목, 역할, 자연어 내용만 입력한다.

## 역할 분류
- 나의 배경(identity): 변하지 않는 배경
- 현재 능력·경험(capability): 난이도를 결정
- 이루고 싶은 결과(objective): 성공 기준을 결정
- 선택·답변 선호(preference): 선택 기준이나 출력 형식을 조정
- 반드시 지켜야 할 조건(hard_limit): 위반하면 답변이 실패
- 가능하면 고려할 조건(soft_limit): 절충 가능한 조건
- 사용 가능한 자원(resource): 시간·예산·도구·교통
- 반복 일정·습관(routine): 실행 계획을 현실화
- 동행자·관계(relationship): 누구와 관련된 질문인지
- 현재 상황(current_state): 지금만 유효한 상태
- 진행 중인 일(project): 연속성이 필요한 작업

## 추천 출력
각 맥락은 must_use / should_use / optional / ignore 중 하나로 분류한다.
관련성 점수뿐 아니라 이유, 답변 변화, 판정 확신도를 함께 표시한다.

## 최종 답변
추천/결론 → 이유 → 실행 단계 → 주의점 → 개인화 반영 순으로 출력한다.
