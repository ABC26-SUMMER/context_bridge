# Context Bridge v20 개선·검증 보고서

## 요구사항 검증

| 요청 | 판단 | v20 반영 |
|---|---|---|
| 승인 정보만 최종 모델 전달 | 필수 | Proposal Snapshot에서 승인 ID 재검증 |
| 거절 카드 값·제목·ID·존재를 prompt/log에서 제거 | 필수 | 생성·수정 prompt와 audit log에서 제거 |
| 민감정보 기본 선택 해제 | 필수 | `suggested=false`, 명시적 양자 선택 |
| 기밀정보 차단 | 필수 | 후보·승인·생성 모두 차단 |
| 모든 hard_limit 강제 포함 제거 | 필수 | 질문의 결정·위험 영역과 연결된 것만 후보 |
| 충돌·신선도 검사 | 필수 | 90일 오래됨, 질문 수치 충돌, 같은 제목 카드 모순 |
| 생성 후 검증·자동 수정 | 필수 | 5개 항목 검사 후 승인 정보만으로 재작성 |
| 기억 자동 저장 금지 | 필수 | `PENDING` 후 `save` 승인 필요 |
| 임베딩 검색 | 조건부 | 실제 벡터 저장소가 없어 제외; 동작을 과장하지 않음 |
| 심사위원 점수 화면 | 선택 | 기본 화면에서는 제거; 내부 계약에는 진단값 유지 |

## 주요 코드 변경

- `backend/server/answerGuard.ts`: 거절 정보 누출, 필수조건 누락, 질문 미응답, 개인정보 반복 검사와 안전한 재작성
- `backend/server/contextIntegrity.ts`: 오래됨·현재 질문 충돌·카드 간 모순 검사
- `backend/server/selection.ts`: 질문 구조화, 관련 위험 영역 hard limit, shortlist 14개 상한
- `backend/server/core.ts`: 민감정보 기본 선택 해제, 검토 필요 카드 자동 선택 금지
- `server.ts`: 승인 전 외부 카드 랭커 제거, 승인 전용 생성 경계, 승인 전용 감사 로그
- `src/components/Sidebar.tsx`: 민감정보 “[사용할게요] [사용하지 않을게요]”
- `src/components/ChatWorkspace.tsx`: 2단계 문구, 쉬운 모드 음성·읽기·되돌리기·최종 확인
- `contracts/types.ts`: 공통 타입에 질문 구조와 무결성 상태 추가

## 실패와 재수정

첫 실행에서 57개 중 11개가 실패했습니다. 실패 원인은 기존 테스트가 “모든 `hard_limit`을 질문과 무관하게 강제 포함”하고 구형 selection mode를 기대했기 때문입니다. 이 기대는 새 개인정보 최소화 정책과 정면으로 충돌했습니다.

재수정:

1. 무관한 hard limit은 제외하는 테스트로 교체
2. 외출·장소 결정 영역과 연결되는 이동·교통·예산 위험만 보호
3. shortlist 상한을 14로 고정
4. selection mode를 `local-hybrid`로 통일
5. 12개 요청 시나리오와 생성 후 검증 회귀 테스트 추가

재실행 결과는 70/70 통과입니다.

## 기존 방식과 개선 방식

| 항목 | 기존 | v20 |
|---|---|---|
| 승인 전 의미 선별 | 카드 일부를 외부 LLM에 전달 | 카드 값은 서버 밖으로 보내지 않음 |
| hard limit | 전부 후보 강제 포함 | 관련 결정·위험 영역만 |
| 민감정보 | 관련되면 기본 선택 가능 | 항상 기본 선택 해제 |
| 거절 정보 감사 | 전체 평가와 Vault 개수 기록 | 승인 평가와 승인 개수만 기록 |
| 생성 결과 | 모델 출력 즉시 표시 | 로컬 누출 검사 후 필요 시 자동 수정 |
| 오래됨·충돌 | 표시 또는 검사 부족 | 자동 선택 차단 후 사용자 확인 |
| 새 기억 | 후보 생성 | 사용자 `save` 전에는 저장하지 않음 |
| UI | 내부 역할·점수 노출 | 사용자용 2단계 문구와 명시적 민감 승인 |

## 실제 실행 검증

```text
npm test        → 7 files, 70 tests passed
npm run lint    → TypeScript passed
npm run build   → Vite production build passed
```

선별 골드 fixture 비교:

- 결정론 F1: 57.1% → 66.7%
- 결정론 정밀도: 73.7% → 75.0%
- 결정론 재현율: 46.7% → 60.0%
- 치명적 누락: 1 → 0
- 동일 mock ranker 구조 비교 F1: 71.2% → 72.7%
- 동일 mock ranker 과선별: 17 → 5

mock ranker 평가는 구조 비교용이며 운영 외부 모델 성능을 의미하지 않습니다. 실제 운영 경로는 승인 전 카드 값을 외부 랭커에 전달하지 않습니다.

정적 검토만 한 항목: 실제 Gemini 운영 품질, 실제 브라우저의 음성 인식 지원 범위, 실제 Supabase/Vercel 배포. 이번 검증은 주입 generator·로컬 test auth·production build를 사용했으며 외부 운영 계정에는 변경을 가하지 않았습니다.
