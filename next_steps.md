# Context Bridge Next Steps

## 현재 상태

Context Bridge는 이제 단일 HTML 프로토타입에서 React/Vite/TypeScript/Tailwind 구조로 이전됐다.

현재 앱의 핵심 흐름은 다음과 같다.

```text
짧은 자연어 입력
→ 질문 의도 분석
→ 필요한 사용자 맥락 선택
→ Context Preview에서 사용자 승인
→ 승인된 맥락만 포함한 고급 프롬프트 생성
→ Prompt Quality로 개선 지점 표시
```

핵심 산출물은 AI의 최종 답변이 아니라, 다른 AI에 그대로 넣을 수 있는 고급 프롬프트다.

## 바로 다음에 할 일

### 1. UI 접근성 정리

목표:

모든 사용자가 읽고 조작하기 쉬운 기본 인터페이스로 만든다.

작업:

- 과한 그림자, 장식 배경, 시각적 노이즈 제거
- 버튼, 체크박스, 입력창의 크기와 간격 정리
- 모바일 화면에서 Context Preview와 Prompt Quality가 읽기 좋게 정리
- 큰 글씨 모드와 기본 모드의 기준 정하기

현재 조치:

- 카드 그림자 제거 완료
- 격자 배경 제거 완료
- 흰 배경과 얇은 경계선 중심으로 단순화 완료
- 프로필별 UI 모드 1차 구현 완료
- 쉬운 모드에서 큰 글씨, 큰 버튼, 짧은 Preview 문구 적용 완료
- 기본 모드에서 기존 정보 밀도 유지 완료

### 2. 사용자 맞춤 인터페이스 모드

목표:

사용자 유형에 따라 같은 기능도 다른 복잡도로 보여준다.

모드 예시:

| 모드 | 대상 | 화면 방식 |
| --- | --- | --- |
| 쉬운 모드 | 고령자, AI 초보자, 어린이 | 큰 글씨, 적은 정보, 단계별 버튼 |
| 기본 모드 | 일반 사용자 | 현재 수준의 Preview와 비교 화면 |
| 상세 모드 | 발표/심사/고급 사용자 | 선택 이유, 민감도, 품질 분석까지 자세히 표시 |

우선 구현 후보:

- 프로필별 기본 UI 모드 지정 완료
- `김하늘`, `박민준` 프로필은 쉬운 모드 적용 완료
- `이지윤`, `최서연` 프로필은 기본 모드 적용 완료
- 쉬운 모드에서는 Prompt Quality를 더 짧은 문장으로 표시 완료

### 3. Prompt Quality 고도화

목표:

원문 입력과 고급 프롬프트의 차이를 더 설득력 있게 보여준다.

현재 표시:

- 원문 입력의 빈칸
- 추가된 프롬프트 역량

추가할 항목:

- 목적 명확성
- 사용자 맥락 포함 여부
- 제약조건 포함 여부
- 출력 형식 포함 여부
- 개인정보 통제 여부

예시:

```text
원문 입력: "이번 방학에 뭐 공부해야 해?"

부족한 점:
- 전공 정보 없음
- 진로 목표 없음
- 하루 학습 가능 시간 없음
- 원하는 출력 형식 없음

개선된 점:
- 백엔드 개발자 목표 반영
- 하루 3시간 제약 반영
- 4주 계획표 형식 지정
```

### 4. 규칙 기반 로직을 LLM API로 교체

목표:

현재는 데모용 규칙 기반이다. 실제 서비스에 가까워지려면 LLM이 질문 의도와 필요한 맥락을 판단해야 한다.

교체 대상:

- `src/services/intentAnalyzer.ts`
- `src/services/contextSelector.ts`
- `src/services/promptComposer.ts`

LLM 적용 후 흐름:

```text
사용자 질문
→ LLM이 intent와 required_context를 JSON으로 반환
→ Pydantic 또는 Zod로 검증
→ Context Preview 표시
→ 승인된 정보만 Prompt Composer에 전달
```

주의:

- 승인되지 않은 정보는 절대 LLM 프롬프트에 포함하지 않는다.
- 민감정보는 별도 표시와 명시적 승인 후에만 포함한다.

### 5. 프롬프트 템플릿 일반화

목표:

현재는 데모 질문별 템플릿이 고정돼 있다. 다음 단계에서는 어떤 질문이 들어와도 구조화된 고급 프롬프트를 만들 수 있어야 한다.

기본 템플릿:

```text
너는 [역할]이야.

사용자의 원래 질문:
"[원문 질문]"

사용자 맥락:
- [승인된 맥락 1]
- [승인된 맥락 2]

제약조건:
- [승인된 제약조건]

출력 형식:
1. [원하는 형식]
2. [주의할 점]

중요:
사용자가 승인하지 않은 정보는 사용하지 마.
```

### 6. 데모 발표 흐름 정리

목표:

심사자가 1분 안에 서비스 차별점을 이해하게 만든다.

발표 흐름:

1. 문제 제기
   - AI를 잘 쓰는 사람은 좋은 입력을 만든다.
   - 그렇지 못한 사람은 같은 AI를 써도 결과가 나쁘다.

2. 짧은 입력 예시
   - “딸이랑 내일 어디 가지?”

3. Context Bridge 작동
   - 필요한 맥락 선택
   - 민감정보 표시
   - 사용자 승인

4. 결과
   - 답변이 아니라 고급 프롬프트 생성
   - 다른 AI에 넣으면 더 좋은 결과를 얻을 수 있음

5. 포용적 AI 연결
   - 프롬프트 작성 능력 차이를 줄인다.
   - 고령자, 어린이, 비전공자, 디지털 취약계층도 고성능 AI를 더 잘 쓸 수 있게 한다.

## 우선순위

### P0

- Prompt Quality 문구 추가 개선
- 쉬운 모드 모바일 화면 점검
- 상세 모드의 발표용 표시 항목 정의

### P1

- LLM API 기반 intent/context selection
- 고급 프롬프트 템플릿 일반화
- 사용 기록 저장 구조 개선

### P2

- 음성 입력
- 다국어 지원
- 보호자 승인 기반 어린이 모드
- 고대비 모드
- 실제 사용자 테스트
