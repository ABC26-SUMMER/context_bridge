# Context Bridge v20 선별·보안 성능

## 실제 동작

- 질문에서 원하는 결과, 현재 결정, 조건, 위험 영역, 시간 범위, 접근성·안전 영역을 구조화합니다.
- 카드 값은 외부 랭커로 보내지 않고 서버 내부의 어휘·형태 정규화·최근 맥락·위험 영역 연결로 후보를 만듭니다.
- `hard_limit`은 전부 강제 포함하지 않습니다. 질문의 결정·위험 영역과 연결된 카드만 후보가 됩니다.
- 후보는 Vault가 100개를 넘어도 최대 14개입니다.
- 기밀정보는 후보와 답변 생성에서 차단됩니다.
- 민감정보는 관련돼도 기본 선택하지 않습니다.
- 생성 뒤 거절 정보 누출, 승인된 필수조건 누락, 질문 미응답, 개인정보 반복을 검사하고 자동 수정합니다.

## v20 실행 검증

| 검증 | 결과 |
|---|---:|
| Vitest | 70/70 통과 |
| TypeScript | 통과 |
| Vite production build | 통과 |
| shortlist 상한 | 14 |
| 150개 카드 성능 회귀 | 통과 |
| 거절 민감정보 prompt/log/answer 부재 | 통과 |

골드 fixture 기반 결정론 비교는 F1 57.1% → 66.7%, 정밀도 73.7% → 75.0%, 재현율 46.7% → 60.0%, 치명적 누락 1 → 0입니다. 동일 mock ranker를 쓴 구조 비교는 F1 71.2% → 72.7%, 과선별 17 → 5였습니다. mock ranker 수치는 운영 Gemini 품질을 뜻하지 않으며, 운영 경로는 승인 전 외부 카드 랭커를 사용하지 않습니다.

임베딩 검색은 현재 구현하지 않았습니다. 벡터 인덱스 없이 임베딩을 사용한다고 표시하면 설명과 동작이 달라지기 때문입니다. 실제 임베딩 저장소를 연결할 때에도 승인 전 카드 값이 외부 서비스로 나가지 않도록 로컬 또는 신뢰 경계 내부의 인덱스를 사용해야 합니다.
