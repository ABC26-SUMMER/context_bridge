# 승인 기반 기억 구조 v2

## 채택한 방식

경진대회에서 안정적으로 점수를 내는 **하이브리드 앙상블** 방식을 적용했다.

1. 결정론적 고재현율 추출기: LLM 장애·타임아웃에도 자주 쓰이는 개인 사실을 후보로 만든다.
2. LLM 1 의미 추출기: 규칙에 없는 새로운 표현과 긴 문장의 원자 사실을 보완한다.
3. 병합·정규화 계층: sourceQuote, 의미축, 내용으로 중복을 제거한다.
4. LLM 2 검증기: CREATE / UPDATE / DROP, 카테고리, 장기성, 자기 정보 여부를 일괄 판정한다.
5. 사용자 승인: 자동 저장하지 않고 최종 저장·갱신은 사용자가 선택한다.
6. 원자적 저장: UPDATE는 기존 context_cards의 ID를 유지한 채 값을 교체한다.

## 이번 수정

- 작은 글씨 접근성, 복약, 디지털 활용, 반복 일정, 근무 형태, 보유 장비, 반려동물, 언어 선호, 프로젝트 역할, 연구, 통학 패턴을 오프라인 폴백에 추가했다.
- `하지만/이지만 + 오늘·이번·지금` 경계를 분리해 사실과 요청 명령이 한 카드에 섞이지 않게 했다.
- 토익 등 단일 최신값 슬롯은 `operation=UPDATE`, `updateTargetId`를 생성한다.
- Supabase RPC가 UPDATE 후보 승인 시 새 카드를 추가하지 않고 기존 카드를 갱신하도록 변경했다.
- 16개 실패 사례를 중심으로 다양성 회귀 테스트를 추가했다.

## 운영 권장값

- LLM 추출 타임아웃: 2.5~4초. 실패 시 즉시 규칙 후보로 진행한다.
- LLM 검증 타임아웃: 2~3초. 실패 시 안전 필터를 통과한 후보를 사용자 승인 화면에 남긴다.
- 후보 최대 수: 8개 유지.
- 평가 지표: 사실 단위 Precision / Recall / F1, 원자성, UPDATE 정확도, 타인정보 오탐률, 일회성 오탐률, p95 지연시간.
- 데이터 분할: 표현 템플릿이 아니라 의미 시나리오 단위 Group split을 사용한다.
