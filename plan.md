# Context Bridge 통합 계획 v10

## 목표

기존 React UI/UX를 유지하면서 다음 구현 과정을 심사위원에게 보인다.

`로그인 계정 → Supabase 프로필/카드 → 서버 규칙·민감정보 필터 → 선택 근거 UI → 승인 Snapshot → LLM`

## 대표 계정

| 계정 | 초기 설정 | 시연 핵심 |
|---|---|---|
| 전이현 | AI·SW학과 대학생 | 진로·학습시간·예산·장소·답변 방식 |
| 김영자 | 고령 사용자 | 디지털 숙련도·이동 접근성·쉬운 설명 |

두 계정 모두 시연에 필요한 최소 가상 데이터만 사용한다. `persona_type`은 초기
UX용이며 규칙 엔진 입력으로 사용하지 않는다.

## 구현 원칙

- 서버가 Auth 토큰을 검증하고 DB에서 계정 소유 데이터를 직접 조회
- RLS와 복합 외래키로 행 접근과 교차 소유자 참조를 함께 차단
- 후보화 단계 외부 AI 호출 금지
- 생성 API에서 질문·카드 값 재수신 금지
- Proposal 소유자·상태·승인 ID 검증
- normal만 기본 선택, sensitive·stale은 기본 해제, confidential은 승인 불가
- 건강·접근성 민감도 서버 강제
- 사용 승인과 장기 기억 저장 승인 분리

## 완료

- Supabase Auth 로그인/세션 연동 및 로컬 데모 폴백
- 계정별 프로필·카드 CRUD와 RLS 스키마
- 전이현/김영자 안전 데모 seed
- 서버 DB 조회 기반 규칙 엔진
- Proposal/Snapshot/감사/기억 후보 DB 저장
- 기존 Preview·비교·Vault·감사·기억·신선도 UX 유지
- 교차 계정 차단 포함 13개 회귀/API 테스트

## 다음 순서

1. 실제 Supabase 프로젝트에서 migration과 seed 실행
2. 두 계정으로 같은 질문을 교차 시연하고 DB 행 격리 확인
3. Gemini 비교 답변 라이브 리허설 및 녹화
4. Playwright E2E와 2~3명 사용성 테스트
5. DB 기록을 단일 트랜잭션/RPC로 묶고 Proposal 재시작 복구
