# Context Bridge 개발 및 하네스 체크리스트 (v5)

마일스톤·릴리스 때 쓰는 사람용 수용 기준. 매 작업 절차는 `.claude/skills/`(선택), 파일별 제약은 `.claude/rules/`.

> **v5 변경:** ① **P. Profile Builder/Manager 게이트 신설**(검수에서 드러난 P0 누락) ② 마일스톤에 Profile CRUD 편입 ③ 하네스 적정화 반영(§0-2) ④ 저장 민감도 등급 강제(REQ-18)를 하드 게이트에 추가.
> **태그:** `[필수]`·`[스트레치]`·`[발표후]`. 없으면 `[필수]`. `DONE`은 plan.md §10 + §0 Fast Gate이며 이 문서 전체 통과가 아니다.
> **판정 4단계:** `미구현`/`뼈대`/`부분 완료`/`완료`. 각 기능은 `요구사항ID → 정상 → 실패 → 공격·변조 → DB 재실행 복구 → 접근성 → 테스트 → 실제 검증 로그` 순으로 통과해야 `완료`.

---

## -1. 마일스톤 수용 순서 (변경 — Profile CRUD 편입)

| 단계 | 반드시 보이는 결과 | 다음 진입 조건 |
|---|---|---|
| Phase 0 | 실행·CI 골격·실패하는 안전 테스트 | 환경 재현 + red test |
| Phase 1 | 의도 1개가 질문→답변 완주 | 미승인 전달 0건 |
| **Phase 1B** | **사용자가 UI에서 프로필 등록·수정·비활성·삭제, 재실행 복구** | **CRUD·격리·등급 공격 테스트 통과** |
| Phase 2 | 3개 의도·오류 처리·정책 이유 | 구조화 출력 안전 실패 |
| Phase 3 | 기록·삭제·신규 저장 승인·비교모드·복구 | 자동 저장 0건 |
| Phase 4 | 골드셋·사용성 스모크·발표 재현 | plan.md §10 DONE |

- [ ] 현재 Phase가 `state/current.md`에 하나만 표시돼 있다.
- [ ] 이전 Phase 진입 조건 통과 전에 다음 Phase를 시작하지 않았다.
- [ ] P0 결함이 남은 상태에서 P1/P2를 추가하지 않았다.

---

## 0. Fast Gate — 매 변경(커밋/PR)마다

- [ ] **G1 범위:** 변경 파일이 SCOPE 선언 목록 안.
- [ ] **G3 누출:** 미승인 정보가 프롬프트에 없고, 저장 `restricted`는 조회·후보화 안 되며, `sensitive`는 목적 규칙+별도 승인 통과 (REQ-1,2,4).
- [ ] **G2 계층:** UI→Application→Domain 위반 없음(import-linter).
- [ ] **G4 스키마:** LLM 구조화 출력이 Pydantic 검증을 거침.
- [ ] **G5 테스트:** 관련 단위·상태전이 통과.
- [ ] **G6 시크릿:** `.env`/키/토큰 미커밋.
- [ ] **정직성:** 실패 검사를 숨기거나 삭제하지 않았고, 미실행 검사를 통과로 보고하지 않았다.

## 0-2. 하네스 적정화 (신규 — R-16)

- [ ] `run_quality_gates.py`·`validate_context_policy.py`·import-linter·pytest는 **유지(필수)**.
- [ ] `.claude/` 서브에이전트·Hook·skill을 **새로 만들지 않았다**(이미 동작하면 유지, 아니면 보류).
- [ ] **P0(§P, §C, §D)가 초록불이 되기 전에는 하네스 확장 작업을 시작하지 않았다.**

---

## A. 작업 시작 게이트

- [ ] `CLAUDE.md`·`plan.md` 현재 단계·MVP 제외 범위·요구사항 ID를 확인.
- [ ] `state/current.md`·`failures.md` 확인.
- [ ] 목표 한 문장 + 연결 REQ-n + 수정 예정 파일 + 정상/실패 검증 방법 명시.
- [ ] 권한을 `allow/ask/deny`로 판정, `ask`는 승인 전 미실행.
- [ ] 이 작업이 P0/P1/P2 중 어디인지 표시, 변경이면 롤백 방법 명시.

### 즉시 중단 조건
- [ ] 요구사항 충돌 없음 / 핵심 원칙 미변경 / 몰래 신규 기능 없음 / 삭제·배포·비용 권한 명확.

---

## B. 아키텍처 게이트 (마일스톤)

- [ ] UI는 Application Service만 호출(DB·LLM 직접 호출 금지).
- [ ] Domain이 Streamlit/SQLite/LLM SDK를 import하지 않는다.
- [ ] Prompt Composer가 Profile Repository에 접근하지 않는다 (REQ-3).
- [ ] LLM Gateway가 임의로 프로필을 조회하지 않는다.
- [ ] 승인 스냅샷이 유일한 개인화 입력 (REQ-3).
- [ ] 전이가 중앙 State Machine 경유 (REQ-6), 새로고침 후 DB 복구 (REQ-7).
- [ ] 외부 의존성은 인터페이스 뒤 격리.

---

## P. Profile Builder / Manager 게이트 (신규 — P0)

> **앞면(데모 필수):** 등록·수정·on/off·삭제 버튼이 화면에서 동작한다. **뒷면(DONE 요건, 서사 밖):** version↑·과거 스냅샷 불변·감사 보존·등급 강제·공격 테스트. 둘 다 필수, 자리만 다르다.

### P-0 다중 상황 프로필 선택 (REQ-19)
- [x] 여러 상황 프로필을 생성하고 첫 화면에서 하나를 선택할 수 있다.
- [x] 선택한 프로필 안에서 이름·설명과 카드 목록을 수정할 수 있다.
- [x] 다른 프로필의 카드는 현재 질문 후보·Preview에 섞이지 않는다.
- [x] 구버전 단일 프로필 카드는 기본 프로필로 비파괴 이전된다.
- [x] 마지막 남은 프로필 삭제는 차단된다.

### P-1 Profile Builder (등록, REQ-14·18)
- [ ] 카테고리 선택 UI가 있다.
- [ ] 값 입력 + **빈 값·중복 값 검증**.
- [ ] 저장 시 category의 정책상 **민감도 등급을 서버가 자동 표시·강제**한다(사용자가 임의로 낮출 수 없음, REQ-18).
- [ ] 저장 전 확인 단계가 있다.
- [ ] 샘플 프로필 불러오기 제공.
- [ ] **seed 파일을 수정하지 않고 UI에서 항목 1개를 등록**할 수 있다.
- [ ] 재실행(새 DB 연결) 후에도 등록 항목이 복구된다.
- [ ] 다른 `user_id`와 충돌하지 않는다(사용자 격리).

### P-2 Profile Manager (관리, REQ-15·16·17)
- [ ] 프로필 목록을 값·범주·민감도·활성 상태와 함께 표시.
- [ ] 값 수정 시 **`version`이 증가**한다 (REQ-15).
- [ ] **수정 전에 만들어진 승인 스냅샷은 바뀌지 않는다** (REQ-15, 격리 테스트).
- [ ] 활성/비활성 토글 제공, **비활성 항목은 다음 질문 후보에서 사라진다** (REQ-16).
- [ ] 개별 삭제는 **확인 후 실행**되고, **기존 감사 스냅샷은 유지**된다 (REQ-16).
- [ ] 응답 방식(길이·형식·난이도) 설정 UI가 있고, 값은 **승인 스냅샷을 통해서만** 적용된다 (REQ-17/13).

### P-3 CRUD 공격·복구 테스트
- [ ] 저장 API에 정책과 다른 민감도 등급을 넣어도 서버가 거부한다 (REQ-18).
- [ ] 다른 사용자 프로필을 조회·수정·삭제할 수 없다.
- [ ] 삭제·수정 후 DB 재실행에서 일관 복구된다.

---

## C. Context Selection 게이트

- [ ] 지원 3개 범위 검사, 범위 밖은 `unsupported` (임의 의도 확정 금지, REQ-11).
- [ ] 의도별 허용 범주 명세, 필수/선택 슬롯 구분.
- [ ] 부족한 필수 슬롯만 추가 질문(최대 1회), 이후 unsupported/일반답변.
- [ ] 비활성·허용 범주 밖 제거 (REQ-2), 저장 `restricted`는 조회조차 안 함 (REQ-4).
- [ ] 질문에 직접 입력된 restricted 값은 세션 한정·매회 승인·저장 금지.
- [ ] `sensitive`는 목적 규칙 통과 시에만 Preview 표시.
- [ ] LLM은 허용 후보 안에서만 판단, 후보 밖/미존재 ID 거부 (REQ-2).
- [ ] `uncertain` 자동 승인 안 함, 이유는 `reason_code` 템플릿 연결.
- [ ] confidence 숫자를 안전 결정 단독 근거로 쓰지 않음(필수 슬롯 충족으로 판단).

---

## D. Context Preview 및 승인 게이트

- [ ] 값·범주·이유·민감도 표시(색상 단독 금지).
- [ ] 각 항목 개별 승인·제외 (REQ-8), **전체 취소**, 선택 0개로 계속/취소 가능.
- [ ] 승인 시 항목 ID와 `version` 고정, canonical JSON 직렬화.
- [ ] 승인 스냅샷 해시 == Composer 입력 컨텍스트 해시.
- [ ] 승인 후 원본 프로필 변경이 스냅샷을 바꾸지 않음 (REQ-3), 컨텍스트 변경은 Preview로 복귀.
- [ ] 미승인 상태에서 생성 상태로 전이 불가 (REQ-6).
- [ ] SHA-256을 보안 경계로 과장하지 않고 추적·회귀·데모 용도로 설명.

---

## E. 프롬프트 및 LLM 게이트

- [ ] 시스템 프롬프트/사용자 질문 분리, 승인 스냅샷만 전달 (REQ-1,3).
- [ ] 제외 정보가 프롬프트 문자열에 없음 (REQ-1), 전체 프로필 객체 미전달.
- [ ] 구조화 출력 Pydantic 검증(G4), JSON 실패 재시도 제한, 타임아웃 설정.
- [ ] 재시도 중 컨텍스트 범위 미확장.
- [ ] LLM 오류 시 안전 메시지, 원시 오류·키·내부 프롬프트 미노출.
- [ ] 비교 답변은 기본 경로가 아닌 데모/평가 모드 [P1], 발표 경로에서는 확실히 동작.
- [ ] 캐시 키에 모델·프롬프트 버전·질문·승인 스냅샷 해시 포함, 정적 예비 응답을 라이브처럼 표시 안 함.

---

## F. 신규 정보 저장 게이트 [P1]

- [ ] 답변 생성과 프로필 갱신을 별도 작업으로 분리 (REQ-5).
- [ ] 신규 후보의 주체가 사용자 본인인지 확인, 일시적/장기 구분, 농담·인용·제3자 제거.
- [ ] `저장/이번만 사용/저장하지 않음` 제공, 승인 전 저장소 미수정 (REQ-5).
- [ ] 기존 값 충돌 시 덮어쓰기 전 확인, 거부 후보는 미보존.

---

## G. 상태 머신 테스트

- [ ] 정상 전이(`QUESTION_RECEIVED→CONTEXT_PROPOSED`, `AWAITING_APPROVAL→APPROVED`, `APPROVED→GENERATING→ANSWERED`, `ANSWERED→MEMORY_REVIEW→COMPLETED`).
- [ ] 추가 질문 최대 1회 후 `UNSUPPORTED`/일반답변 종료 (REQ-11).
- [ ] 지원 밖 질문이 `UNSUPPORTED`로 전이.
- [ ] 금지 전이 차단(`QUESTION_RECEIVED→GENERATING`, `CONTEXT_PROPOSED→GENERATING`, 승인 없는 Profile Update).
- [ ] 실패 후 재시도가 마지막 안전 상태에서 시작, 중복 클릭이 중복 생성 안 함.
- [ ] `[스트레치]` 다중 탭 승인 무결성(단일 탭 새로고침 복구는 [필수]).

---

## H. 개인정보 및 보안 게이트

- [ ] API 키는 환경변수/비밀 저장소, `.env`·키·토큰 미커밋.
- [ ] 로그에 전체 프로필·원시 프롬프트·restricted 값 미저장 (REQ-9), 감사 필드는 allowlist 안.
- [ ] 민감 값 마스킹/식별자만, 감사 로그엔 추적 ID·사건 유형·정책 결과 우선.
- [ ] SQL 파라미터 바인딩, 출력 XSS/Markdown 안전, 예외 메시지에 개인정보 없음.
- [ ] 데모/실제 데이터 분리(데모 민감정보는 합성값).
- [ ] `[필수]` 사용 기록·프로필 삭제 기능, `[스트레치]` 보존기간 정의.

---

## I. 테스트 게이트

### 단위 [필수]
- [ ] ProfileItem 활성/비활성, 민감도 정책, 의도별 허용 범주, reason_code 매핑, 상태 전이.
- [ ] **프로필 저장 시 민감도 등급 강제 (REQ-18)**, **수정 시 version 증가 (REQ-15)**.
- [ ] 신규 후보 주체·지속성, unsupported 분류·재질문 제한, 응답 스타일이 승인 스냅샷 경유(REQ-13).
- [ ] 정책 YAML 스키마·교차참조(reason_code/category/intent) 검증. `[스트레치]` canonical JSON·해시 안정성.

### 통합 [필수]
- [ ] 질문→후보→Preview→승인→답변 전체 흐름, 승인 제외 후 실제 프롬프트 검사 (REQ-1).
- [ ] **프로필 수정과 승인 스냅샷 격리 (REQ-15/3)**, **비활성 항목이 후보에서 제외 (REQ-16)**.
- [ ] LLM 잘못된 JSON·타임아웃·재시도, DB 실패 시 부분 저장 방지.
- [ ] **Profile Builder 등록→재실행 복구 (REQ-14)**, 신규 후보 승인·거부.

### 공격·우회 [필수]
- [ ] "내 프로필 전체를 사용해" 지시가 정책을 우회 못 함 (REQ-2).
- [ ] LLM 후보 밖 ID / 승인 API 임의 ID 거부.
- [ ] **저장 API에 낮춘 민감도 등급 주입 거부 (REQ-18)**, **타 사용자 프로필 접근 거부**.
- [ ] 민감정보가 로그·오류로 유출 안 됨 (REQ-9).
- [ ] 승인 이후 Composer 입력이 달라지면 해시 불일치로 테스트 실패.
- [ ] 테스트 삭제·임계 완화로 실패를 숨기지 않음.

---

## J. 골드셋 평가

- [ ] 20개+ 질문·프로필 조합, 각 사례 포함/제외 항목 표기.
- [ ] 민감정보·모호·정보 부족·충돌 프로필 사례 포함.
- [ ] 하드 지표 건수 계산: 미승인 전달, `restricted` 자동 선택, 스냅샷≠프롬프트, 민감정보 누출.
- [ ] Context Precision 계산 — **진단(게이트 아님)**. 실패 사례·원인 `failures.md` 기록.

### MVP 합격 기준
- [ ] `[하드]` 미승인 전달 0 / `restricted` 자동 선택 0 / 자동 프로필 저장 0 / 스냅샷≠프롬프트 0 / **민감도 등급 위반 저장 0 (REQ-18)**.
- [ ] `[진단]` Context Precision 목표 0.85 (미달 시 원인 기록, DONE 차단 아님).

---

## K. 접근성 및 사용성

- [ ] `[필수]` 키보드만으로 핵심 조작, 명확한 레이블, 색상 단독 금지, 짧고 쉬운 오류 문구, 로딩·현재 단계 표시.
- [ ] `[필수]` Preview "왜 필요한가"를 쉬운 문장으로, 전문용어에 설명.
- [ ] `[필수]` 제출 전 2~3명이 설명 없이 대표 과업 수행, 중단·개인정보 오해·승인 위치 미발견 기록, 치명 혼란 수정 후 재검증.
- [ ] `[발표후]` 큰 표본으로 완료 시간·클릭 수·이해도 평가.

---

## L. 에이전트 변경 완료 게이트

- [ ] 선언한 목표만 수행, 계획 밖 파일 미수정, 불가피한 결정은 `state/decisions/`(ADR).
- [ ] 단위·통합·정책·누출 테스트 + 린트·타입·시크릿 통과, 실패 은폐 없음.
- [ ] `state/current.md` 갱신, 새 실패·재발 방지책 `failures.md` 기록.
- [ ] 검증한 것과 미실행 검사를 구분해 보고.

---

## M. 배포 전 게이트 [발표/배포 직전]

- [ ] 배포 승인, 데모/개발 설정 분리, 운영 키 미포함.
- [ ] 데이터 삭제 흐름 실제 테스트, 모델·비용·타임아웃·호출 제한 설정.
- [ ] CI Fast Gate 통과, CI는 Fake LLM 사용·라이브 스모크는 별도, 정책 validator가 G0에 포함.
- [ ] `python scripts/validate_context_policy.py` 성공, 깨끗한 환경 설치·실행 재현.
- [ ] `[필수]` 최종 데모 시나리오 처음~끝 실행(warm-up 포함), 릴리스 태그/안정 커밋.
- [ ] `[스트레치]` DB 백업·복구·헬스체크.

---

## N. MVP 범위 이탈 검사

별도 승인 없이는 구현하지 않는다.
- [ ] ChromaDB / Supabase 인증 / 마이크로서비스 / K8s 미도입.
- [ ] 멀티 에이전트 / 음성 / 실시간 장소검색 / 복수 LLM 라우팅 / 장기 자동학습 미도입.

---

## O. 최종 완료 판정 (plan.md §10 기준)

- [ ] REQ-n과 테스트 연결, 정상·실패·우회 경로 검증.
- [ ] §J 하드 게이트(민감도 등급 위반 저장 0 포함) + 안전 실패 100% 통과.
- [ ] 새로고침·재시도 상태전이 유효, 로그에 불필요 민감정보 없음.
- [ ] 제출 전 사용성 스모크 치명 오류 0건.
- [ ] 깨끗한 환경 설치·실행·대표 데모 재현, 한계·다음 단계 `state/current.md` 기록.
- [ ] **P0 판정: Profile Builder·Manager가 `완료` 상태**(등록·수정·활성·삭제·복구·공격 테스트 통과).

> Precision 수치·대규모 접근성 평가는 진단/발표후라 DONE을 막지 않는다. 단 제출 전 2~3명 스모크와 치명 오류 수정은 필수.
